import { describe, it } from 'vitest';

/**
 * ValidationPipe e2e — full Vitest e2e runner lands in Track 6 (070).
 * Stub kept here so the HTTP surface is tracked from step 032.
 */
describe('ValidationPipe (e2e stub)', () => {
  it.todo('rejects unknown body fields with 400');
});

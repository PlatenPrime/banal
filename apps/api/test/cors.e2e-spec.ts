import { describe, it } from 'vitest';

/**
 * CORS preflight e2e — full Vitest e2e runner lands in Track 6 (070).
 * Stub kept here so the HTTP surface is tracked from step 015.
 */
describe('CORS (e2e stub)', () => {
  it.todo('allows OPTIONS preflight from WEB_ORIGIN');
});

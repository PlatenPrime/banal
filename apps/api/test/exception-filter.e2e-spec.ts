import { describe, it } from 'vitest';

/**
 * ApiExceptionFilter e2e — full Vitest e2e runner lands in Track 6 (070).
 * Stub kept here so the HTTP error surface is tracked from step 033.
 */
describe('ApiExceptionFilter (e2e stub)', () => {
  it.todo('returns application/problem+json without stack');
});
